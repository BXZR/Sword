using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class forStart : MonoBehaviour {

	public GameStartButton theButton;

	void Update () {
		if (Input.anyKeyDown) 
		{
			theButton.makeStart ();
			Destroy (this.gameObject);
		}
	}
}
