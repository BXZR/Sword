using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerStar : MonoBehaviour {

	//这个类用来记录游戏人物的各个属性评分
	//用于显示在多边形图状态图中
	//这是一个额外的组件，不予playerBasic有关
	//所有的评价目前都由人定
	//评分大小 0 - 0.99

	//以下两项单独编辑
	//对应UIStateShowImage的 values ValueTitles;
	//这两个List的数据长度需要一致

	public List<string> theTitles;
	public List<float> theValues;

}
